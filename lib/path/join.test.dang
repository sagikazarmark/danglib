{
  assert { join([]) == "" }
  assert { join(["", ""]) == "" }
  assert { join(["", "a", "", "b", ""]) == "a/b" }
  assert { join(["/a", "b", "..", "c"]) == "/a/c" }
  assert { join(["a", "..", "b"]) == "b" }
  assert { join([" ", "a"]) == " /a" }
}
